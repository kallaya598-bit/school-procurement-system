services:
  - type: web
    name: school-procurement-system
    env: python
    buildCommand: apt-get install -y libreoffice-writer && pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: HOST
        value: 0.0.0.0

